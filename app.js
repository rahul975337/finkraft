const fun = (arr, sum) => {
   let map ={}
   for (let i = 0; i < arr.length; i++) {
    let left = target-arr[i]
   }
  return res;
};

let arr = [1,3,10,11,14];
let sum = 13
let res = fun(arr,sum)

for (let i = 0; i < res.length; i++) {
    const element = res[i];
    console.log(element);

}